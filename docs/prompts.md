整理输入的修改prompts

1. 使用pencil生成[ui@nju-timetable.pen](mailto:ui@nju-timetable.pen) ，要求可参考@pencil-continue-prompt.txt ，并检查这个prompt里的页面是否已经全部涵盖@requirements document.md 的需求，并且稍作修改：默认不要深色，改成浅色，但是风格一致
2. 1. 修改手动添加课程界面的ui，使其与其他界面相似度更高
  1. 修改课表界面，保持和整体一致的风格，做成表格形式，ui主打高级简约，背景图全覆盖，选择一张简洁高级的背景图作为示例，课程块做成液态玻璃效果，保证文字清晰可见，并支持背景图片、全局主题色、课表透明度自定义，支持为每门课程设置独立颜色标记，便于视觉区分。默认会为不同课程生成不同颜色。可在后续ui界面设置配色方案。提供课程快速编辑、删除、复制、移动功能，支持拖拽调整时间。ui保持圆角风格
  2. 增加“我的”页面，支持头像，基本信息，个性签名，设置等等（需要你进行补充）社交平台所需配置
  3. 设计底部ui栏，有：课表，功能，我的这三个
    .在每个页面左上角放上返回图标
  4. 增加功能界面，里面放置各个功能的进入模块
3. 1. 去除课表页的背景定制，将课表页改为相同的风格
  1. 将功能中的导入与同步，手动添加课程删除，并在课表界面右上角放一个加号，点击加号出现下拉界面，下拉界面包括“导入课表”，“界面修改”，“全部课程”
  2. 手动添加课程新增“是否显示在课表”（因为有的课是自己看网课）
  3. 新增全部课程界面，可以在这里看到所有的课程及相关信息，包括不显示在课表上的课程，点击可进入课程详情页
  4. 课程详情页右上角新增编辑按钮（铅笔ui），进入编辑模式后铅笔ui换成打勾（表示完成）。右上角添加进入课程论坛的按钮
  5. 功能包括以下：学期总览、上课提醒、考试与作业、专注模式、习惯养成、待办、日志、课程社区，每个占一位
  6. 删除智能助手这个页面
  7. 学期总览修改功能，改成一个日历的形式，考试，放假用不同颜色的框标出，支持手动添加重要的日子
  8. 删除云端备份的功能
4. 改一些小细节
  1. 课表界面左侧节数延伸到12节课，第7节16:10-17:00，第8节17:10-18:00，第9节18:30-19:20，后面计算课间10分钟上课50分钟
  2. 课表界面点击加号后出现的下拉界面和加号靠近一点，并放置在最顶层，宽度减小贴近文字，加号右侧放一个设置图标，“我的课表”放大并居中，月份上移，第几周放到月份下面
  3. 我的课表右侧加一个下拉箭头，点击可选择看谁的课表，默认查看自己的课表
  4. 所有返回均下移，预留出状态栏的位置不要放东西
  5. 删除除课表/功能/我的三个界面以外的界面的底部胶囊
5. 1. 把课表界面的加号和设置也放到状态栏下，并给加号和我的课表旁边的箭头做下拉的展开/收起双状态
  1. 导入课表界面的多份列表，删除好友，同学按钮，改为一个编辑（铅笔）按钮，点击编辑 进入"___ 的"的编辑，编辑完同样形成一个胶囊
  2. 上课提醒界面同理，做成可自主编辑的胶囊
  3. 我的界面，头像换成默认用户头像（你做一个），删除学号，删除微信qq，apple,google等登录方式，加入设置（放在隐私与体验），个人信息编辑（账号与社交）的按钮，账号与社交上方加一个栏，横着放（带图标） 我的点赞，我的收藏，我的关注
  4. 全部课程界面，，将课程信息（时间，地点，老师）放在课程名下（灰体小字）
  5. 统一所有界面的标头的字体大小和位置
6. 1. 修复学期总览里日历显示的问题，一天一个日期，并显示月份，加入一个月的所有天并可切换月份（左右箭头按钮），每个颜色的框显示它的含义
  1. 删除功能中的专注模式，习惯养成，删除专注与习惯界面
  2. 新增待办界面
  3. 新增我的日志界面
  4. 新增日志编辑界面
7. 1. 删除课表界面的”展开收起““课表选择 修改”那一行，修改课程方框的显示：下方灰体小字第一行放地点就好了，宽度增大，两列之间几乎没有间隔
  1. 我的日志右上角的新建改成胶囊按钮，图标为加号，待办的右上角也放加号，日志编辑的右上角完成做成胶囊的打勾，日历界面的添加也做成图标加号胶囊，
  2. 日历下方加上日期，居中加粗，今日日期主题色标注，重要日不要框框标注，改为实色填充
  3. 底部胶囊ui文字放大
8. 1. 把课表里的课程块再做一版“标准双列栅格”（固定列宽 + 自动对齐到网格线），让课程密集时也保持整齐，左侧节数和上方日期断开，改为圆角毛玻璃质感
  1. 把设置里的第一行四个选项拆开成四行
  2. 我的日志右上角新建改成主题色胶囊，待办的新建改为和我的日志的新建同样的款式和位置
9. 课表的显示不对，没有横竖对齐，一节课跨两天了
  课程社区的讨论左侧显示发布者头像，日期，讨论区右上角新增红色胶囊新建按钮
  新增讨论区文章界面，含标题，作者（作者有红色+关注按钮），日期，右下角有点赞，收藏，评论，左下角可以勾选是否匿名回复下方有评论区，有楼中楼回复，大概像这样
  新建发布讨论区文章编辑界面，可以勾选是否匿名发布
10. 评论也显示头像，时间，右侧可点赞评论，同样生效于楼中楼

添加可以对评论贴表情和文章贴表情，如果是点击他人评论，可以直接回复，并显示"回复“谁
11. 把课程社区的评价维度下面的几个评价分行展示
资料右上角加一个红色胶囊+上传按钮
日历的考试，放假做成实色填充，略放大今日日期，选用一个颜色深一点的灰色表示今日（实色填充），把左右调整月份放到日期那行
12. 日历上的今日换成深灰色边框描边吧，实色填充太丑了，下方今日的按钮做成白底的
发布讨论的添加图片胶囊改成正文右下角一个图片的ui，选择课程放到正文的左下角，以话题#标出
往课表里多填点课程我看看效果
13. 你排布的课表和左侧课时没有对齐，你最好将他做成隐形网格的形式，让课表只要一添加就是和表格对齐的
14. 把设置里的开关做成左右拨动按钮式，
将应用主题色的按钮（现为红色）处的颜色值弄出一个接口，方便填入一个色值之后更换所有的主题色颜色
并告诉我如何修改
15. 把黑色字体，白色圆角框和背景也更换成变量，方便设置深色模式，并请你修改一个有高级感的深色模式给我看
16. 首先，把深色模式的课表界面的课表上的字体单独拿出来，白色有点不易读，改成一个梗易读的颜色（或者黑色字体加白色阴影）
，然后，添加一个变量dark_mode，当为true的时候设置为这些值（不影响主题色），false的时候为一开始的值（图片供你参考）
17. 修复：深色模式的讨论区的楼中楼显示出现问题
课表界面的周一意外出现颜色不一致
以及我没有找到深浅色模式的切换入口
18. 将课程社区界面中的评价维度，资料整合到课程详情中
将课程社区，讨论详情，发布讨论这三个界面放到另一个pen文件中，做成将来可能扩展的功能，现在我们先不做
19. 0. 接下来，你新增的界面的风格应该始终和其他一致，并将使用到的颜色使用variable里面的对应变量，方便暗色模式切换，所有“是否”的选项都用滑动控制，样式和设置中的一致，记住这一点
    1. 新增待办详情界面，右上角是编辑主题色胶囊按钮，可以填写标题/内容/地点/待办种类(（三个可选，被选中的打勾）（1）是否是有持续时间的待办，（如果选择了这个，下方时间填写格式为起始），（2）是否是有ddl的待办，（如果选择了这个，下方填写格式为ddl：）（3）普通待办（不显示时间）)/重要程度（有三档可以选，使用图形化ui）/是否提醒（和提醒时间）/进度日志，
    2. 修改待办界面，在（全部，未完成，已完成）这一行下面放排序方式（默认/按添加时间排序），（注: 默认即重要程度高且紧急程度赶的显示在前面），在右上角新建的右边放一个胶囊按钮（批量编辑）
    3. 待办的右侧，如果是有持续时间的待办，显示起始时间。如果是有ddl的待办，右侧显示ddl时间和右侧放一个完成的胶囊按钮，如果是普通待办，显示一个完成的按钮
    4. 完成的待办设置划线删除的效果，全部视图中已完成的待办将放在最底下
    5. 日历下方可以显示当天有标注时间的待办
 20. 待办界面的排序做成小字的一行，旁边的默认和按添加时间不要做成胶囊，做成简单的文字选择即可（默认/按添加时间，选择哪个哪个字体变成主题色），这一行不要太显眼
  统一所有完成的按钮的形式，为以主题色为底色。起始时间的显示颜色改为主题色
21.1. 待办列表行左侧使用数字标注待办顺序
    2. 设置中的外观配置新增“重新随机生成课表配色方案”
    3. 修改课表视图配色风格， @requirements document.md:19 
    4. 考试与作业右上角增加+新建胶囊按钮
    5. 新增添加考试与作业的界面，可选择添加类型：考试/作业，考试则编辑时间，地点，关联的课程，考试类型（自行填写，期中/期末/小测等），备注，作业则设置ddl（按周几的几点来选，可选多天），关联的内容，是否重复（采用滑钮ui）
    6. 新增查看考试/作业详情的界面，点击具体的考试与作业可进入，右上角可编辑
    7. 待办底下新增一个可勾选的按钮“关联考试与作业”，勾选后自动将考试与作业添加到待办，并在以后新建考试和作业的条目的时候直接同步到待办
    8. 删除“我的”界面，将底部胶囊“我的”改为“待办”，并在待办界面添加底部胶囊，在功能中删除待办
    9. 根据这次的prompt更新@requirements document.md 
22.功能页的底部胶囊不对
  已完成的待办不要加入序号排序，但是字体位置和上面的对齐
  关联考试与作业放在页面底部，不要使用白框，灰色小字，右侧有一个圆圈可以勾选
23. 日志与删除、日历擦除（按顺序落实原型与文档）
  1. 日志详情页（`17 Log Editor`）在标题区下方展示**记录时间**，格式含**日期与时间，精确到秒**（与列表一致的数据字段）。
  2. 「我的日志」列表（`16 My Logs`）每条摘要上方的时间行改为**含秒的完整时间**。
  3. 在以下页面**内容区最底部**增加**删除**入口（样式为白底/表面色圆角框 + 主题色文案，与现有变量体系一致）：**课程详情**（文案建议「删除课程」）、**考试与作业详情**（「删除本条记录」或等价文案）、**待办详情**（「删除待办」）、**日志详情**（「删除日志」）。删除前须**二次确认**（弹窗），删除后返回上一页并刷新列表/课表等关联数据。
  4. 学期日历（`10 Semester Overview`）：当某日存在**重要日 / 放假 / 考试**等**手动或可编辑类标注**时，在下方信息区展示对应说明；在该说明区域**下方**增加按钮**「擦除特殊日期标记」**（用于清除用户为当日设置的重要日等标记；**自动同步的考试日期**是否一并清除由数据模型约定，须在实现时区分「仅清手动标记」与「同步源仍在则次日仍会回填」等情况，并在文档中写明）。
  5. 自查其余数据是否缺少删除入口：**全部课程列表**、**课表课程块**均通过进入**课程详情**删除课程即可；**待办列表**已有批量编辑下的删除；**日志列表**未要求行内删除，以详情页删除为准；**多份课表**的删除/重命名在导入与课表管理流程中处理（若原型未单独画「删除课表」按钮，实现时补全）；**课程详情—资料**中已上传文件建议**每条资料**提供删除或等价操作（若原型仅有列表，实现时补行内删除/菜单）。
  6. 将本条约束同步写入 `requirements document.md` 与 `frontend-functional-spec.md`。
24. 课程资料行内删除与设置课表管理（并固化文档同步流程）
  1. **课程详情—资料**：在**编辑模式**下，每条已上传资料文件名**右侧**显示**主题色小叉号（lucide `x`，颜色 `$theme.accent`）**，点击删除该文件（可与二次确认策略一致）；浏览模式不显示叉号。
  2. **设置**：新增 **「课表管理」** 区块，列出多份课表，每行名称右侧为主题色 **×**，用于删除该份课表；**至少保留一份课表**时须拦截删除；新建/改名课表仍在导入与课表相关流程处理。
  3. **项目规则**：以后每次改动产品/原型，必须按顺序：**①** 在 `prompts.md` 末尾追加**下一条编号**的变更说明；**②** 同步更新 `requirements document.md`；**③** 同步更新 `frontend-functional-spec.md`。已将此流程写入仓库 **`.cursor/rules`**（文档同步规则），后续 Agent/协作需遵守。
25. 设置 **课表管理** 中，**「本人课表」**（默认主课表）这一行**不显示**右侧删除 ×；仅**其余课表行**保留主题色 × 删除；说明文案与 `requirements document.md`、`frontend-functional-spec.md` 同步。
26. 在 `docs/` 下新增延伸文档（与功能规格、需求**低重复**），定稿文件名：`frontend-data-and-state-contract.md`（实体与字段、不变量、页面—状态订阅、导入 API 占位）、`frontend-engineering-layout.md`（路由—原型映射、目录与组件分层）、`frontend-acceptance-matrix.md`（可勾选验收矩阵）；`frontend-functional-spec.md` 尾部链接上述三份；`requirements document.md` 文档维护节增加索引。
27. 新增 `docs/backend-design.md`：可选后端分期（导入网关 / 远期云同步）、与 `CourseDraft` 及本地契约对齐、认证与 HTTP API 纲要（无代码）；同步更新 `data-and-state-contract.md`、`frontend-data-and-state-contract.md`、`requirements document.md`（多端与云端节 + 文档维护）、`frontend-functional-spec.md` 延伸表。
28. **工程落地（app/mobile）**：在 Monorepo 中实现 `app/mobile` 主路径 UI（课表周视图+冲突分栏、三 Tab、二级页路由）、`expo-sqlite` 本地快照持久化与启动 hydration、`ThemeProvider` 变量化主题、课程/待办/考试作业/日志/日历/设置/导入 Mock 等；开发过程与验证记录写入 `docs/DEV_LOG.md`。不涉及新产品范围变更时，不强制改写需求三章，仅补充工程日志与 prompts 本条。
